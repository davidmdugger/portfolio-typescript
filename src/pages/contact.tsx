import React from "react";
import Layout from "../components/layout";

export default (): React.ReactElement => (
  <div>
    <Layout location={window.location} title="yp">
      <h1>Contact</h1>
    </Layout>
  </div>
);
